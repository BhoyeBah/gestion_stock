# Laravel standard
/vendor/
node_modules/
npm-debug.log
yarn-error.log
yarn-debug.log
pnpm-lock.yaml

# Composer & build artifacts
composer.phar
/.idea/
/.vscode/
*.iml

# Environment files
.env
.env.*.local
.env.backup
.env.testing
.env.local

# Application storage & logs (garder .gitkeep si nécessaire)
storage/*.key
/storage/app/public
/storage/*.log
/storage/debugbar
/storage/framework/cache/data
/storage/framework/sessions
/storage/framework/testing
/storage/framework/views
/storage/logs

# Public storage symlink (you should create it in deploy)
public/storage

# Cache & compiled files
/bootstrap/cache/*.php
/public/hot
/public/storage
/public/mix-manifest.json
/public/js/*.map
/public/css/*.map

# IDEs, editors, OS files
*.swp
*~
.DS_Store
Thumbs.db
desktop.ini

# Node / frontend build
/public/hot
/public/storage
/public/mix-manifest.json
resources/js/dist
resources/css/dist

# SASS / CSS caches
.sass-cache
.cache/
.parcel-cache/

# PHPUnit & test artifacts
phpunit.xml
phpunit.xml.dist
/.phpunit.result.cache

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
docker-compose.override.yml

# Secrets, keys
/*.key
/.vagrant
Homestead.yaml
Homestead.json

# VS Code settings
.vscode/*

# JetBrains IDEs
.idea/*
*.iml

# Laravel Sail
vendor/bin/sail

# Optional: don't commit composer.lock for libraries but keep for apps if you want to lock deps.
# If you prefer to track composer.lock, remove the next line.
# /composer.lock

# Optional: vendor can be committed for deployment without composer, but usually it's ignored:
# /vendor

# Local dotenv override for CI (if you use)
.env.testing.local
.env.production.local

# Mac specific
.DS_Store

# Windows specific
Thumbs.db
ehthumbs.db
